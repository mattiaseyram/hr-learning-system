language: node_js

node_js: 
  - "stable"

branches:
  only:
    - master

before_script:
  - npm install -g firebase-tools
  - npm install -g create-react-app
  - npm install

script:
  - npm run travisDeploy